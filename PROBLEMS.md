# 推荐题目清单

> 用法：做完一道就把 `[ ]` 改成 `[x]`
> 原则：**按顺序做，不要跳题**。每题都要写注释四要素（链接/思路/复杂度/易错点）。

---

## 阶段一：双指针巩固（与已做的 27 题同源）

- [ ] **283. 移动零**（简单）—— 双指针，把非零元素往前搬，末尾补零
- [ ] **344. 反转字符串**（简单）—— 双指针，左右对撞交换
- [ ] **125. 验证回文串**（简单）—— 双指针 + 字符串过滤（只保留字母数字）

> 这三道和你做过的 027「移除元素」是同一个套路，做完你会对双指针彻底形成肌肉记忆。

---

## 阶段二：字典与集合（用上 P62~P82 学的数据容器）

- [ ] **217. 存在重复元素**（简单）—— 用 `set` 去重后比长度
- [ ] **387. 字符串中的第一个唯一字符**（简单）—— 用 `dict` 统计每个字符出现次数
- [ ] **242. 有效的字母异位词**（简单）—— 两个 `dict` 计数比对

> 这三道是「用哈希表换时间」的入门，也是你 001 题哈希表解法的延伸。

---

## 阶段三：一次遍历的技巧

- [ ] **121. 买卖股票的最佳时机**（简单）—— 边走边记录"历史最低价"
- [ ] **136. 只出现一次的数字**（简单）—— 异或运算（或 `set` 求和技巧）
- [ ] **169. 多数元素**（简单）—— 哈希计数 / 摩尔投票法
- [ ] **88. 合并两个有序数组**（简单）—— 双指针**从后往前**填，避免覆盖

---

## 阶段四：进阶挑战（做完上面 10 道再试）

- [ ] **53. 最大子数组和**（中等）—— 一次遍历，维护"以当前位置结尾的最大和"

---

## ⏸ 暂时不要碰（需要「类」，等学完 P111~P126 面向对象）

| 类别 | 题目 |
|---|---|
| 链表 | 206. 反转链表、21. 合并两个有序链表、141. 环形链表 |
| 树 | 94. 二叉树的中序遍历、104. 二叉树的最大深度 |

> 不是它们难，而是你现在**还没学怎么定义 `class`**。等第 3 周学完面向对象，这些题会立刻变得简单。

---

## 每日节奏

- 每天 **2 道**，做完立刻按 `TEMPLATE.md` 的流程提交
- 一周做完这 10 道 + 1 道挑战题，进度刚刚好
